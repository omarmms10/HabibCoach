@model IEnumerable<HabibCoach.Models.DailyRoutine>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProgramId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoutineTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoutineNote)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoutineTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoutineNote)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DailyRoutineId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DailyRoutineId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DailyRoutineId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
